in time;

use std::time::_;
use std::conv;

__test {
    let z = (1).seconds ().to![c8] ();
    assert (z == "1s");

    let x = Duration (w-> 1, d-> 12);
    assert (x.weeks == 2 && x.days == 5);
}


__test {
    let start = Instant::now ();
    sleep ((10).millis ());

    let end = Instant::now ();
    let res = end - start;

    assert (res > (10).millis () && res < (11).millis ());
}
