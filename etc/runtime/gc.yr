mod etc::runtime::gc;

/**
 * Disable the gargabe collector
 */
pub extern (C) def _yrt_disable_GC ();

/**
 * Enable the garbage collector if it was disabled
 */
pub extern (C) def _yrt_enable_GC ();
