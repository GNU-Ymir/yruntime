mod core::comparison;
import std::io;

pub def opEquals {struct T} (a : T, b : T)-> bool {
    cte for i in 0u32 .. typeof ((__pragma!tupleof (a)))::arity {        
        if ((__pragma!tupleof (a)).(i) != (__pragma!tupleof (b)).(i)) return false;
    }

    return true;
}
