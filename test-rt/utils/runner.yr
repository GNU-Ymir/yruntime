/*
 *
 * <hr>
 *
 * @Package: ymirc
 * @Module: utils::runner
 * @File: utils/runner.yr
 * @Author(s):
 *   - Emile Cadorel <ecadorel@gmail.com>
 *
 * @Created(YYYY-MM-DD): 2026-02-01
 * @Copyright(C) 2021–2026 GNU-Ymir
 * @License
 *   ```text
 *   This file is part of the Ymir language project.
 *
 *   Ymir is free software: you can redistribute it and/or modify it
 *   under the terms of the GNU General Public License as published by
 *   the Free Software Foundation, either version 3 of the License, or
 *   (at your option) any later version.
 *
 *   Ymir is distributed in the hope that it will be useful,
 *   but WITHOUT ANY WARRANTY; without even the implied warranty of
 *   MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
 *   GNU General Public License for more details.
 *
 *   You should have received a copy of the GNU General Public License
 *   along with Ymir.  If not, see <https://www.gnu.org/licenses/>.
 *   ```
 */

in runner;

use std::{io, config::_, algorithm::searching, fs::_};

use core::exception::_;
use utils::{colors, filters, args, worker};
use utils::coverage::{tree, store, load, format, conv};

extern (C) fn _yrt_force_debug()-> void;

@{final}
pub class UnittestLauncher {

    // The list of test registered in the launcher This function pointers are
    // cheated by the compiler as they don't respect the non throwing
    // convention, as they throw when they fail. It is the standard accepted
    // way, as it is only for internal usage.
    //
    let dmut _tests: [[c8] => fn ()-> void] = copy [];

    /*!
     * ====================================================================================================
     * ====================================================================================================
     * ======================================          CTOR          ======================================
     * ====================================================================================================
     * ====================================================================================================
     */

    pub self () {}

    /*!
     * ====================================================================================================
     * ====================================================================================================
     * ==================================          REGISTRATION          ==================================
     * ====================================================================================================
     * ====================================================================================================
     */

    /**
     * Register a test in the unittest map
     * @params:
     *    - name: the name of the test to register
     *    - func: the function to execute for the test
     *
     * */
    pub fn register(mut self, name: [c8], func: fn ()-> void) {
        self._tests[name] = func;
    }

    /*!
     * ====================================================================================================
     * ====================================================================================================
     * =====================================          USAGE          ======================================
     * ====================================================================================================
     * ====================================================================================================
     */

    /**
     * Run the tests

     * @params:
     *    - args: the list of parameters passed to the program
     *    - covTree: the code coverage tree that is being filled when branch of code are hit
     *
     * @returns:
     *     true iff all test successfully passed
     * */
    pub fn run(self, args: [[c8]], covTree: &CoverageTree)-> bool {
        let testArgs = {
            self.parseArguments(args)
        } catch {
            _ => { return false; } // parsing failed
        };

        self.resetCoverageUnlessResuming(testArgs.hasResume);

        _yrt_force_debug();
        let dmut already: [[c8] => bool] = copy [];

        let toLaunch = TestFilters::filterList(copy [name for name, _ in self._tests],
                                               hasFilters-> testArgs.hasFilter,
                                               filter-> testArgs.filter,
                                               resume-> testArgs.hasResume,
                                               already-> ref already);

        let failed = if testArgs.jobs <= 1 || toLaunch.len <= 1us {
            self.runList(toLaunch, testArgs.hasStopFirst, ref already)
        } else {
            self.runParallel(toLaunch, testArgs, covTree, ref already)
        };

        TestFilters::dumpSuccessFile(already);
        self.storeCoverage(covTree);

        if !failed {
            self.reportCoverage(testArgs);
        }

        return !failed;
    }

    /**
     * Run every test of `tests` sequentially, in this process, recording each one's outcome
     * into `already`
     *
     * @params:
     *    - tests: the names of the tests to run
     *    - hasStopFirst: true iff execution must stop at the first failure
     *    - already: the map to record each test's success/failure into
     *
     * @returns: true iff at least one test of `tests` failed
     * */
    fn runList(self, tests: [[c8]], hasStopFirst: bool, ref dmut already: [[c8] => bool])-> bool {
        let mut failed = false;
        for test in tests {
            if let Ok(f) = self._tests[test] {
                {
                    self.runTest(test, f);
                    already[test] = true;
                } catch {
                    err: &Exception => {
                        self.exposeError(test, err);
                        already[test] = false;
                        failed = true;
                        if (hasStopFirst) break;
                    }
                }
            }
        }

        return failed;
    }

    /**
     * Split `toLaunch` into `testArgs.jobs` chunks(see `TestChunks::split`), fork one
     * `TestWorker` subprocess per chunk, wait for all of them, and merge their results into
     * `already`
     *
     * @info: worker stdout/stderr is inherited and unpiped, so it freely interleaves across
     *        workers -- this is the accepted, expected behaviour of a parallel test runner
     *
     * @params:
     *    - toLaunch: the names of the tests to run, split across workers
     *    - testArgs: the parsed arguments of this run
     *    - covTree: the code coverage tree that will be filled by each worker's own tests
     *    - already: the map to merge the workers' results into
     *
     * @returns: true iff at least one worker reported a failure
     * */
    fn runParallel(self, toLaunch: [[c8]], testArgs: TestRunnerArgument, covTree: &CoverageTree, ref dmut already: [[c8] => bool])-> bool {
        TestFilters::clearStaleFragments();
        let chunks = TestChunks::split(toLaunch, testArgs.jobs);
        let dmut pids: [u32] = [];

        for chunk in chunks {
            if let Ok(pid) = TestWorker::fork() {
                pids ~= [pid];
            } else {
                self.runWorkerChunk(chunk, testArgs.hasStopFirst, covTree); // never returns
            }
        }

        return self.collectWorkers(pids, testArgs.hasStopFirst, ref already);
    }

    /**
     * The body of a forked worker: run its chunk, persist its results(its own success fragment
     * and coverage file), then terminate. Never returns.
     *
     * @params:
     *    - chunk: the names of the tests this worker must run
     *    - hasStopFirst: true iff execution must stop at the first failure
     *    - covTree: this(forked) process's own code coverage tree
     * */
    fn runWorkerChunk(self, chunk: [[c8]], hasStopFirst: bool, covTree: &CoverageTree) {
        let dmut mine: [[c8] => bool] = copy [];
        let failed = self.runList(chunk, hasStopFirst, ref mine);

        TestFilters::dumpSuccessFragment(mine);
        self.storeCoverage(covTree);

        TestWorker::exit(if failed { 1 } else { 0 });
    }

    /**
     * Wait for every worker of `pids` to terminate, then merge their success fragments into
     * `already`(see `TestFilters::mergeFragments`)
     *
     * Workers are reaped in completion order rather than in `pids` order(see
     * `TestWorker::waitAny`), so that a failure is observed as soon as it happens and not only
     * once the pid this was blocked on finally exits. When `hasStopFirst` is set, the first
     * failure cancels every sibling still running(see `cancelWorkers`); the loop then keeps
     * reaping so that no worker is left a zombie.
     *
     * @params:
     *    - pids: the pid of every forked worker(see `TestWorker::fork`)
     *    - hasStopFirst: true iff a single worker's failure must cancel all the others
     *    - already: the map to merge the workers' results into
     *
     * @returns: true iff at least one worker exited with a non-zero status
     * */
    fn collectWorkers(self, pids: [u32], hasStopFirst: bool, ref dmut already: [[c8] => bool])-> bool {
        let dmut running: [u32 => bool] = copy [];
        for pid in pids {
            running[pid] = true;
        }

        let mut failed = false, mut cancelled: [u32] = [];
        for _ in 0us .. pids.len {
            let (pid, code) = TestWorker::waitAny();
            if (pid == 0u32) break; // no child left to reap

            running[pid] = false;
            if (code != 0) {
                failed = true;
                if (hasStopFirst && cancelled.len == 0us) {
                    cancelled = self.cancelWorkers(running);
                }
            }
        }

        self.discardWorkerFiles(cancelled);
        TestFilters::mergeFragments(ref already);

        return failed;
    }

    /**
     * Kill every worker still flagged as running in `running`(see `TestWorker::kill`) -- called
     * under `--stop-first` once one worker has already failed, so the others do not keep running
     * tests whose result the run will discard anyway
     *
     * @params:
     *    - running: whether each forked worker is still running(false once it has been reaped)
     *
     * @returns: the pid of every worker that was killed
     * */
    fn cancelWorkers(self, running: [u32 => bool])-> [u32] {
        let dmut killed: [u32] = [];
        for pid, isRunning in running {
            if (isRunning) {
                TestWorker::kill(pid);
                killed ~= [pid];
            }
        }

        return killed;
    }

    /**
     * Delete the success fragment and coverage file of every worker of `pids`
     *
     * A killed worker(see `cancelWorkers`) did not choose its moment to die: it may have been
     * halfway through writing either file, and a truncated one would either be merged as bogus
     * results or make a later `--resume` run fail to parse it. Its chunk did not run to
     * completion anyway, so the safe reading of both files is "no results at all" -- the tests
     * it did pass are simply run again next time.
     *
     * @params:
     *    - pids: the pid of every worker that was cancelled
     * */
    fn discardWorkerFiles(self, pids: [u32]) {
        for pid in pids {
            for path in [CoverageStore::pidPathOf(pid), TestFilters::fragmentPathOf(pid)] {
                {
                    removeFile(Path(path));
                } catch {
                    _ => {} // never written(the usual case): nothing to discard
                }
            }
        }
    }

    /**
     * Delete every coverage file left over from previous runs(see `CoverageStore::PID_FILE_PREFIX`),
     * unless `--resume` was passed, in which case this run's coverage must be merged with theirs
     *
     * @params:
     *    - hasResume: true iff the flag resume was found in argument parser
     * */
    fn resetCoverageUnlessResuming(self, hasResume: bool) {
        if !hasResume {
            CoverageStore::removePidFiles(CoverageLoad::listPidFiles());
        }
    }

    /**
     * Export this process's coverage tree to its own coverage file(see `CoverageStore::storePid`),
     * so it can be merged with the coverage of other runs when the report is printed
     *
     * @params:
     *    - covTree: the code coverage tree that was filled while running the tests
     * */
    fn storeCoverage(self, covTree: &CoverageTree) {
        {
            CoverageStore::storePid(covTree);
        } catch {
            err => { println("err ", err); }
        }
    }

    /**
     * Print the coverage/call-tree report, merged from every coverage file found on disk(this
     * run's, plus any left over from a previous `--resume` run)
     *
     * @params:
     *    - testArgs: the parsed arguments of this run
     * */
    fn reportCoverage(self, testArgs: TestRunnerArgument) {
        let report = CoverageConv::fromConfigs(CoverageLoad::loadAll(CoverageLoad::listPidFiles()));

        CoverageFormat::report(report, testArgs.hasCoverage, testArgs.hasCallTree, withMissing-> testArgs.hasMissing);
    }

    /**
     * Run a unittest
     *
     * @params:
     *     - name: the name of the test
     *     - func: the function of the unit test
     *
     * @throws:
     *    - the exception thrown by the test if it fails
     * */
    fn runTest(self, name: [c8], func: fn ()-> void)
        throws Exception
    {
        println('[', Colors::BLUE, "RUN", Colors::RESET, "] : ", name);
        println("====================================");
        self.fake_throw(true);
        func();
        println("====================================");
        println('[', Colors::GREEN, "SUCCESS", Colors::RESET, "] : ", name);
    }

    /**
     * Print the error of a test when it failed
     *
     * @params:
     *     - name: the name of the test that failed
     *     - err: the exception thrown by the test
     *
     * */
    fn exposeError(self, name: [c8], err: &Exception) {
        println("====================================");
        println('[', Colors::RED, "FAILURE", Colors::RESET, "] : ", name);

        println(" --> ", err);
    }

    /*!
     * ====================================================================================================
     * ====================================================================================================
     * ====================================          PRIVATE          =====================================
     * ====================================================================================================
     * ====================================================================================================
     */

    /**
     * Some cheating util, to make the compiler accept the try/catch block, when the actual throwing part is the unittes.
     *
     * */
    fn fake_throw(self, b: bool)
        throws AssertError
    {
        assert(b);
    }

    /**
     * Parse the arguments passed to the launcher
     *
     * */
    fn parseArguments(self, args: [[c8]])-> TestRunnerArgument
        throws AssertError
    {
        return TestRunnerArgument(args);
    }
}
