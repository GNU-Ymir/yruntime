in map;

mod implem;
use std::collection::map::implem;

@final
pub class HashMap {K, V} over IHashMap!{K, V, false} {
    pub self (len : usize = 0us) with super (len-> len) {}
}

@final
pub class HashMap {K, V of dmut Z, class Z} over IHashMap!{K, V, true} {
    pub self (len : usize = 0us) with super (len-> len) {}
}

@final
pub class HashMap {K, V of dmut Z, record Z} over IHashMap!{K, V, true} {
    pub self (len : usize = 0us) with super (len-> len) {}
}
