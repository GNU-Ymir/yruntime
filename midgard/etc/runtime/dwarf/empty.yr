in empty;

@final
pub class EmptyDwarfLoader over DwarfLoader {
    pub self () {}
    pub over find (mut self, _ : [c8], _ : usize)-> ([c8], usize)? {
        none
    }

    pub over find (mut self, _ : [c8], _ : usize, _ : usize, _ : usize)-> ([c8], usize)? {
        none
    }

    pub over dispose (mut self) {}
}
