in map;

@abstract
pub class MapException over Exception {
    pub self () {}
}

/**
 * Exception thrown when a key that does not exist in the map is accessed
 * */
@final
pub class KeyErrMap over MapException {
    pub self () {}
}
