mod core.macro;

public {
    __macro q {
        (*elem: ($i: token)) {
            let cte res = "";
            cte for it in elem
                res = res ~ it.i;
            res;
        }
    }
}

