import core.io;

struct
| a : [int; 10U]
 -> Test;


def print (I : ulong) (a : [int ; I]) {
    print ("[");
    for it in 0U .. a.len {
        print (a [it]);
    }
    print ("]");
}

def main (args) {
    let b = Test::init;// {[1, 2, 3]};
    println (b.a);    
}

