import core.io;

struct
| x : Point
| y : Point
 -> Rectangle;

struct
| x : int
| y : int
 -> Point;

def main () {
    let point = Point {10, 1};
    let rect = Rectangle {point, Point {1, 3}};
    match rect {
        Rectangle {Point {x, _}, Point {y, _}} => println ((x, " ", y));
    }
}
