import core.io;

struct (T ...) 
| foo : fn (T)-> void
 -> Signal;



def print (T, T2) (a : fn (T) -> T2) {    
}

def main () {
    let a = Signal!(int) {print};
    a.foo (12);
    print (a.foo);
}
